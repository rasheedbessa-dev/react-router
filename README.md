{condition ? (...) : (...)}
{condtion1 ? (...) : condition2 ? (...) : (...)}

{condition && (...)}

function test() {

}

const Greeting = () => {

}

## State

const [name, setName] = useState(...)

## Map function

lists.map((listItem, idx) => {
console.log(listItem, idx)
})

list["Ahmed", "Malek", "Mourad"]
